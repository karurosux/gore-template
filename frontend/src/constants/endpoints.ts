export class Endpoints {
  static readonly BASE_V1 = "/api/v1"
  static readonly AUTH = `${Endpoints.BASE_V1}/auth`
  static readonly USER = `${Endpoints.BASE_V1}/users`
  static readonly ROLE = `${Endpoints.BASE_V1}/roles`
  static readonly BRANCH = `${Endpoints.BASE_V1}/branches`
  static readonly PERMISSION = `${Endpoints.BASE_V1}/permissions`
}
